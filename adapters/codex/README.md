# Codex (optional)

Configure `uv run agentic-analytics serve --transport stdio` as an MCP server. This adapter
is convenience documentation only; it contains no schemas, provenance, or validation logic.

